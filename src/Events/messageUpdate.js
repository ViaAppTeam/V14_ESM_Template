import { PermissionsBitField } from "discord.js"

/**
 * @param {import("discord.js").Client} client
 */


export default async (oldMessage, newMessage) => {
//* newMessage - oldMessage Create
};